// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: usage.sql

package queries

import (
	"context"
)

const GetAreaComparisonUsage = `-- name: GetAreaComparisonUsage :one
SELECT id, "userId", month, year, "comparisonsUsed", "lastComparisonAt", "createdAt", "updatedAt"
FROM area_comparison_usage
WHERE "userId" = $1 AND month = $2 AND year = $3
`

type GetAreaComparisonUsageParams struct {
	UserId string `json:"userId"`
	Month  int32  `json:"month"`
	Year   int32  `json:"year"`
}

func (q *Queries) GetAreaComparisonUsage(ctx context.Context, arg GetAreaComparisonUsageParams) (AreaComparisonUsage, error) {
	row := q.db.QueryRow(ctx, GetAreaComparisonUsage, arg.UserId, arg.Month, arg.Year)
	var i AreaComparisonUsage
	err := row.Scan(
		&i.ID,
		&i.UserId,
		&i.Month,
		&i.Year,
		&i.ComparisonsUsed,
		&i.LastComparisonAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const GetInvestmentPlanUsage = `-- name: GetInvestmentPlanUsage :one

SELECT id, "userId", month, year, "picksUsed", "lastPickAt", "createdAt", "updatedAt"
FROM investment_plan_usage
WHERE "userId" = $1 AND month = $2 AND year = $3
`

type GetInvestmentPlanUsageParams struct {
	UserId string `json:"userId"`
	Month  int32  `json:"month"`
	Year   int32  `json:"year"`
}

// Usage Tracking Queries (Investment Plan + Area Comparison)
func (q *Queries) GetInvestmentPlanUsage(ctx context.Context, arg GetInvestmentPlanUsageParams) (InvestmentPlanUsage, error) {
	row := q.db.QueryRow(ctx, GetInvestmentPlanUsage, arg.UserId, arg.Month, arg.Year)
	var i InvestmentPlanUsage
	err := row.Scan(
		&i.ID,
		&i.UserId,
		&i.Month,
		&i.Year,
		&i.PicksUsed,
		&i.LastPickAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const UpsertAreaComparisonUsage = `-- name: UpsertAreaComparisonUsage :one
INSERT INTO area_comparison_usage (
    id, "userId", month, year, "comparisonsUsed", "lastComparisonAt", "createdAt", "updatedAt"
) VALUES ($1, $2, $3, $4, $5, NOW(), NOW(), NOW())
ON CONFLICT ("userId", month, year) DO UPDATE SET
    "comparisonsUsed" = area_comparison_usage."comparisonsUsed" + EXCLUDED."comparisonsUsed",
    "lastComparisonAt" = NOW(),
    "updatedAt" = NOW()
RETURNING id, "userId", month, year, "comparisonsUsed", "lastComparisonAt", "createdAt", "updatedAt"
`

type UpsertAreaComparisonUsageParams struct {
	ID              string `json:"id"`
	UserId          string `json:"userId"`
	Month           int32  `json:"month"`
	Year            int32  `json:"year"`
	ComparisonsUsed int32  `json:"comparisonsUsed"`
}

func (q *Queries) UpsertAreaComparisonUsage(ctx context.Context, arg UpsertAreaComparisonUsageParams) (AreaComparisonUsage, error) {
	row := q.db.QueryRow(ctx, UpsertAreaComparisonUsage,
		arg.ID,
		arg.UserId,
		arg.Month,
		arg.Year,
		arg.ComparisonsUsed,
	)
	var i AreaComparisonUsage
	err := row.Scan(
		&i.ID,
		&i.UserId,
		&i.Month,
		&i.Year,
		&i.ComparisonsUsed,
		&i.LastComparisonAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const UpsertInvestmentPlanUsage = `-- name: UpsertInvestmentPlanUsage :one
INSERT INTO investment_plan_usage (
    id, "userId", month, year, "picksUsed", "lastPickAt", "createdAt", "updatedAt"
) VALUES ($1, $2, $3, $4, $5, NOW(), NOW(), NOW())
ON CONFLICT ("userId", month, year) DO UPDATE SET
    "picksUsed" = investment_plan_usage."picksUsed" + EXCLUDED."picksUsed",
    "lastPickAt" = NOW(),
    "updatedAt" = NOW()
RETURNING id, "userId", month, year, "picksUsed", "lastPickAt", "createdAt", "updatedAt"
`

type UpsertInvestmentPlanUsageParams struct {
	ID        string `json:"id"`
	UserId    string `json:"userId"`
	Month     int32  `json:"month"`
	Year      int32  `json:"year"`
	PicksUsed int32  `json:"picksUsed"`
}

func (q *Queries) UpsertInvestmentPlanUsage(ctx context.Context, arg UpsertInvestmentPlanUsageParams) (InvestmentPlanUsage, error) {
	row := q.db.QueryRow(ctx, UpsertInvestmentPlanUsage,
		arg.ID,
		arg.UserId,
		arg.Month,
		arg.Year,
		arg.PicksUsed,
	)
	var i InvestmentPlanUsage
	err := row.Scan(
		&i.ID,
		&i.UserId,
		&i.Month,
		&i.Year,
		&i.PicksUsed,
		&i.LastPickAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
